@model RssAlerter.Models.AlertSources.RssFeedAlertSource

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

<script src="@Url.Content("~/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)
    <fieldset>
        <legend>RssFeedAlertSource</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.FeedUri)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.FeedUri)
            @Html.ValidationMessageFor(model => model.FeedUri)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.UpdateFrequency)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.UpdateFrequency)
            @Html.ValidationMessageFor(model => model.UpdateFrequency)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
